# CKA Mock Exam Practice Tasks
Disclaimer: This list has been generated by AI

**Time Limit:** 2 hours  
**Passing Score:** 66%  
**Total Tasks:** 17

## Instructions
- You have access to kubernetes.io documentation
- Each task has a weight percentage
- Use `kubectl` and other CLI tools as needed
- Work efficiently - time management is crucial

---

## Task 1: RBAC Configuration (7%)
**Context:** cluster: kubernetes-admin@kubernetes

Create a new ServiceAccount named `app-deployer` in the `production` namespace. Create a Role that allows creating, listing, and deleting Pods and Deployments. Bind this Role to the ServiceAccount.

---

## Task 2: Cluster Upgrade (10%)
**Context:** cluster: kubernetes-admin@kubernetes

Upgrade the control plane node from version 1.28.0 to 1.29.0. Ensure the node is properly drained before upgrade and uncordoned after completion.

**Note:** Worker nodes do not need to be upgraded.

---

## Task 3: Network Policy (7%)
**Context:** cluster: kubernetes-admin@kubernetes

Create a NetworkPolicy named `db-policy` in the `database` namespace that:
- Allows incoming traffic only from pods with label `app=backend` on port 3306
- Allows all outgoing traffic
- Applies to pods with label `app=mysql`

---

## Task 4: Persistent Volume (7%)
**Context:** cluster: kubernetes-admin@kubernetes

Create a PersistentVolume named `pv-analytics` with:
- 2Gi storage
- accessMode: ReadWriteOnce
- hostPath: /data/pv-analytics
- storageClassName: manual

Then create a PersistentVolumeClaim named `pvc-analytics` in namespace `analytics` that uses this PV.

---

## Task 5: Troubleshooting - Pod Failure (13%)
**Context:** cluster: kubernetes-admin@kubernetes

A pod named `web-server` in namespace `development` is failing to start. Identify and fix the issue so the pod runs successfully.

---

## Task 6: Static Pod (4%)
**Context:** cluster: kubernetes-admin@kubernetes

Create a static pod named `critical-app` on node `node01` with:
- Image: nginx:1.21
- Label: tier=critical

---

## Task 7: Multi-container Pod (7%)
**Context:** cluster: kubernetes-admin@kubernetes

Create a pod named `logger-app` in namespace `monitoring` with:
- Container 1: nginx:alpine (name: nginx)
- Container 2: busybox:1.35 (name: sidecar) running `sh -c "tail -f /var/log/nginx/access.log"`
- Shared volume mounted at `/var/log/nginx` for both containers

---

## Task 8: Node Maintenance (4%)
**Context:** cluster: kubernetes-admin@kubernetes

Mark node `node02` as unschedulable and safely drain all pods from it while respecting PodDisruptionBudgets.

---

## Task 9: Service and Endpoint (7%)
**Context:** cluster: kubernetes-admin@kubernetes

Create a Service named `external-db` in namespace `default` that points to an external database at IP `10.0.1.50` on port `5432`. The service should be accessible within the cluster on port `5432`.

---

## Task 10: ConfigMap and Secret (7%)
**Context:** cluster: kubernetes-admin@kubernetes

Create a ConfigMap named `app-config` with key-value pairs:
- `APP_ENV=production`
- `LOG_LEVEL=debug`

Create a Secret named `db-credentials` with:
- `username=admin`
- `password=Sup3rS3cr3t!`

Create a pod named `config-test` using image `busybox:1.35` that mounts both as environment variables.

---

## Task 11: Deployment Scaling and Updates (7%)
**Context:** cluster: kubernetes-admin@kubernetes

Scale the deployment `web-frontend` in namespace `production` to 5 replicas. Then perform a rolling update to change the image to `nginx:1.22` and record the change.

---

## Task 12: Backup and Restore etcd (7%)
**Context:** cluster: kubernetes-admin@kubernetes

Create a backup of etcd and save it to `/tmp/etcd-backup.db`. The etcd server is running on the control plane node at `https://127.0.0.1:2379`.

---

## Task 13: Ingress Configuration (7%)
**Context:** cluster: kubernetes-admin@kubernetes

Create an Ingress resource named `app-ingress` in namespace `production` that:
- Routes traffic from `app.example.com/api` to service `api-service` on port 8080
- Routes traffic from `app.example.com/web` to service `web-service` on port 80

---

## Task 14: DaemonSet (4%)
**Context:** cluster: kubernetes-admin@kubernetes

Create a DaemonSet named `logging-agent` in namespace `kube-system` using image `fluentd:v1.14` that runs on all nodes including control plane nodes.

---

## Task 15: Resource Limits (4%)
**Context:** cluster: kubernetes-admin@kubernetes

Create a pod named `limited-pod` in namespace `default` with:
- Image: nginx:alpine
- CPU request: 100m, CPU limit: 200m
- Memory request: 128Mi, Memory limit: 256Mi

---

## Task 16: Troubleshooting - Node NotReady (7%)
**Context:** cluster: kubernetes-admin@kubernetes

Node `node03` is in NotReady state. SSH into the node, identify the issue with the kubelet, and fix it so the node becomes Ready.

---

## Task 17: Monitoring and Logging (4%)
**Context:** cluster: kubernetes-admin@kubernetes

Find the pod in namespace `production` that is consuming the most CPU. Save the pod name to file `/tmp/high-cpu-pod.txt`.

---

## Tips for Success

1. **Use aliases:** Set up `alias k=kubectl` to save time
2. **Use imperative commands:** Generate YAML with `kubectl run` and `kubectl create` then edit
3. **Check your work:** Always verify your solutions with `kubectl get`, `kubectl describe`
4. **Time management:** Don't spend too much time on one task
5. **Documentation:** Practice navigating kubernetes.io/docs quickly
6. **Context switching:** Remember to switch contexts when specified
7. **Partial credit:** Even partial solutions may earn points

Good luck with your CKA exam preparation!